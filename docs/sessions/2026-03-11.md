# Session 2026-03-11

## Was gemacht wurde

### Projekt-Init
- CLAUDE.md für das Projekt erstellt
- docs/-Struktur angelegt (decisions, todo, ideas, changelog, sessions/)
- Auto-Memory für Claude Code eingerichtet

### Code-Qualität & Tests
- **98 Tests** geschrieben (vorher 19): Unit Tests für sitemap_parser und content_extractor, Integration Tests für den gesamten Crawl-Pipeline, Policy-Discovery Tests
- **ruff** als Linter eingerichtet (ersetzt black/pylint/mypy), alle Fehler gefixt
- **pyproject.toml** mit pytest- und ruff-Konfiguration
- `sys.path.insert` bereinigt: project root statt src/, konsistente `from src.*` Imports
- Inline `import time` nach Modul-Level verschoben für sauberes Mocking

### Features
- **JSON-Reihenfolge**: headings und meta_description kommen jetzt vor full_text – damit Clara die compliance-relevanten Signale sofort sieht
- **Policy-Discovery erweitert**: Shopify, WooCommerce und generische Pfade (22 statt 5)
- **`--shop-type` CLI-Flag**: `shopify`, `woocommerce`, `generic` – ohne Angabe werden alle Pfade probiert

### Dokumentation
- README.md komplett überarbeitet: alle CLI-Optionen als Tabelle, Shop-Type-Erklärung, JSON-Struktur
- docs/USAGE.md synchronisiert

## Entscheidungen
- ruff statt black/pylint/mypy (einfacher, schneller, ein Tool)
- Policy-Pfade als Dict nach Shop-Typ statt Auto-Detection (explizit > magisch)
- JSON-Feld-Reihenfolge: Compliance-Signale vor Fließtext

### Web-Interface (Session 2, Fortsetzung)
- **Flask Web-Interface** gebaut: `python app.py` → Browser öffnet sich auf localhost:8080
- Dark Theme mit Amber-Akzenten, Instrument Serif + DM Sans + JetBrains Mono
- SSE (Server-Sent Events) für Live-Streaming des Crawl-Fortschritts
- Sidebar mit Formular (Domain, Shop-Type, Content-Toggle, Kategorie-Pills, Max URLs)
- **Kategorie-Filter-Bug gefixt**: Checkboxes starteten `checked` ohne `.selected` CSS-Klasse → alle Kategorien wurden unbeabsichtigt exkludiert. Fix: Checkboxes starten unchecked, Reset bei Filtermodus-Wechsel.
- **Clipboard-Fix**: `navigator.clipboard.writeText()` scheitert auf localhost ohne HTTPS → Fallback via `textarea + execCommand('copy')`
- **Zwei separate Copy-Buttons**: "URLs kopieren" und "Content kopieren" statt einem generischen "JSON kopieren"
- Backend gibt jetzt `urls_file` und `content_file` separat zurück (statt einem `file`-Feld)
- "Im Finder öffnen"-Button öffnet den Output-Ordner
- README mit Web-Interface-Workflow und Linting-Pfad aktualisiert

## Entscheidungen
- ruff statt black/pylint/mypy (einfacher, schneller, ein Tool)
- Policy-Pfade als Dict nach Shop-Typ statt Auto-Detection (explizit > magisch)
- JSON-Feld-Reihenfolge: Compliance-Signale vor Fließtext
- Flask + SSE für Web-Interface (statt TUI oder native macOS App)
- Clipboard-Fallback via textarea+execCommand für localhost-Kompatibilität

## Was offen ist
- Retry-Logik bei fehlgeschlagenen HTTP-Requests
- Fortschrittsanzeige bei Content-Extraktion (SSE-Infrastruktur steht, Progress-Events müssen granularer werden)

## Nächste Schritte
- Retry-Logik wäre der sinnvollste nächste Schritt (robusterer Crawl bei instabilen Verbindungen)
- Granulare Fortschrittsanzeige im Web-Interface (URL-für-URL statt nur Endstatistik)
- Optional: Crawl-Historie im Web-Interface
