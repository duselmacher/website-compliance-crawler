# Session 2026-03-15

## Was gemacht wurde
- **Clipboard Pre-Fetch Fix** committed (`aa8b6e2`): JSON-Dateien werden nach Crawl-Ende sofort geladen (pre-fetch), damit der Copy-Button synchron auf die Daten zugreifen kann. Löst das Problem, dass der User-Gesture-Kontext nach einem async `fetch()` verloren geht.
- Änderung war bereits lokal getestet und funktionsfähig

## Entscheidungen
- Pre-Fetch statt async Copy: Dateien werden bei Crawl-Ende geladen, nicht erst beim Klick. Robuster auf localhost ohne HTTPS.

## Was offen ist
- Retry-Logik bei fehlgeschlagenen HTTP-Requests
- Granulare Fortschrittsanzeige im Web-Interface (URL-für-URL)

## Nächste Schritte
- Retry-Logik wäre der sinnvollste nächste Schritt
- Danach granulare Fortschrittsanzeige (SSE-Infrastruktur steht bereits)
